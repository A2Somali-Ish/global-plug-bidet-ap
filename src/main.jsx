import React from 'react'
import ReactDOM from 'react-dom/client'
import GlobalPlugApp from './GlobalPlugApp.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <GlobalPlugApp />
  </React.StrictMode>,
)
